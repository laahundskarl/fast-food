apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2  # Usar o storage class disponível
  resources:
    requests:
      storage: 1Gi  # Mínimo para economizar (~$0.10/mês)
